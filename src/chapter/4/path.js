/**
 * 从G中找到所有起点为s的路径
 */
export default class Path {
  /**
   * 是否存在从s到v的路径
   * @param v
   * @return boolean
   */
  hasPathTo (v) {}

  /**
   * s到v的路径，如果不存在则返回undefined
   * @param v
   * @return Array[number] | undefined
   */
  pathTo (v) {}
}
