export default class BaseST {
  /**
   * 将键值对存入表中
   * @param key
   * @param value
   */
  put (key, value) {}

  /**
   * 获取键key对应的值
   * @param key
   * @return {value}
   */
  get (key) {}

  /**
   * 从表中删去键key
   * @param key
   */
}
